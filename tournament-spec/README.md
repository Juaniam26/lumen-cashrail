# Grok Six-Bot Revenue Tournament v1.8 — Verified Deliverable Package

This archive is a self-contained control and mission packet for the six-bot tournament. It includes the master mission, shared execution kernel, gate-control kit, runtime contract, Jev profit-routing architecture, audit dispositions, implementation sequence, reconstruction disclosure, production-hardening audit, runtime blocker register, manifest, checksums, a structural verifier, and bot mission packet(s).

Read `RECONSTRUCTION-NOTES.md` first. The package is production-ready as a specification and archive, not launch-ready as an operating business. Real provider connections, people, authority, prices, capacity, evidence, and readiness gates remain to be verified. Nothing in this archive authorizes outreach, spend, customer commitments, invoices, charges, or bot activation.

Verify with SHA-256 against `INTEGRITY-SHA256.txt`, inspect `MANIFEST.json`, and run `python3 tools/verify_package.py` before use. Read `PRODUCTION-READINESS-AUDIT.md` and `IMPLEMENTATION-BLOCKERS.md` before treating any runtime claim as verified. Each individual bot packet contains copies of the shared control documents, but this archive does not contain the missing executable service or provider integrations.
